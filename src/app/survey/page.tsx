import { SurveyFormView } from "@/features/survey/SurveyFormView";

export const metadata = {
  title: "Field Survey | TransitFlow AI",
  description: "Submit a new field survey observation",
};

export default function SurveyPage() {
  return <SurveyFormView />;
}
